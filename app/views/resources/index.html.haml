.container
  .content-title
    All resources
  - Resource.all.sort{|a,b|a.title<=>b.title}.each do |r|
    .page-heading-in-page-summary
      %a{href: resource_path(r.id)}
        = r.title

    %a{href: resource_path(r.id)}
      %img{src: r.file, width: '150px'}
    %br

    - if current_user.can_delete?(r)
      .float-right
        = link_to 'Remove resource', r,
             :confirm => 'Are you sure you want to remove this resource? This cannot be undone.',
             :method => :delete
    %hr
