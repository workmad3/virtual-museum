<%= form_for([page, page.comments.build]) do |f| %>

      <%= f.label :commenter %>
      <%= f.text_field :commenter %>

      <%= f.label :content %>
      <%= f.text_area :content %>
      <br/>
      <%= f.submit class: 'btn btn-primary' %>
<% end %>