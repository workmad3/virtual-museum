<h1>Edit <%= page.title %></h1>

<%= render :partial => 'layouts/page_form', :locals => {
        form_submit_url:     update_url,
        form_submit_method:  'put',
        page_title:          page.title,
        page_content:        page.content }
%>
